Patterned after xero's [dotfiles](https://github.com/xero/dotfiles). Uses gnu `stow`

Usage:

```sh
cd ~
git clone https://github.com/drewbarbs/dotfiles.git
cd dotfiles
stow tmux
# later, uninstall
stow -D tmux
```
